<html>
    <body>
        <p>{{.uuid}}</p>
        <p>Your embed link <a href="localhost:8080/e/{{.uuid}}">localhost:8080/e/{{.uuid}}</a></p>
        <p>
            {{ range $index, $elem:= .text }}
                {{ $elem }}<br></br>
            {{ end }}
        </p>
    </body>
</html>